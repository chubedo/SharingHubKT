const path = {
  home: '/',
  login: '/login',
  logout: '/logout',
  register: '/register',
  explore: '/explore',
  notifications: '/notifications',
  profile: '/profile'
} as const

export default path
